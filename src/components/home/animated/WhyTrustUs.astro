<div class="w-full bg-[#f5f7fa] py-[80px] px-6 lg:px-[120px] relative z-[22]">
  <!-- Centered content inside -->
  <div
    class="flex flex-col lg:flex-row max-w-[1440px] mx-auto gap-12 items-center justify-around"
  >
    <!-- Image section -->
    <div class="relative w-full max-w-[576px] h-auto">
      <!-- <div
          class="absolute top-[118px] left-1/2 transform -translate-x-1/2 z-10 w-[50px] h-[50px] bg-[url('https://codia-f2c.s3.us-west-1.amazonaws.com/image/2025-07-26/VnKF0fMc22.png')] bg-cover bg-no-repeat"
        >
        </div> -->
      <div
        class="absolute top-[118px] left-1/2 transform -translate-x-1/2 z-10
         w-[50px] h-[50px] rounded-full
         bg-[url('https://codia-f2c.s3.us-west-1.amazonaws.com/image/2025-07-26/VnKF0fMc22.png')] bg-cover bg-no-repeat
         transition-all duration-300 hover:ring-2 hover:ring-white hover:ring-offset-1 hover:ring-offset-transparent"
      >
      </div>

      <div
        class="w-full h-[300px] rounded-[21px] bg-[rgba(0,0,0,0.12)] bg-[url('https://codia-f2c.s3.us-west-1.amazonaws.com/image/2025-07-26/mjsskrpPBj.png')] bg-cover bg-no-repeat relative z-0"
      >
      </div>
    </div>

    <!-- Text section -->
    <div class="flex flex-col gap-6 w-full max-w-[576px] z-10">
      <h2
        class="why-heading text-[28px] sm:text-[32px] lg:text-[36px] font-bold leading-[1.3] text-[#04092a]"
      >
        Fractional CFO Support
      </h2>
      <p
        class="why-para text-[16px] sm:text-[17px] lg:text-[18px] leading-[27px] text-[#5c667a]"
      >
        At times, your business needs strategic financial leadership! With FinKeepers, you get access to a group of fractional CFOs, available for short or long-term assignments. They bring in deep U.S. market expertise. You get to choose the right professional with CFO-level insights without the overhead of hiring full-time. 

      </p>
    </div>
  </div>
</div>

<script>
  import { gsap } from "gsap";
  import { TextPlugin } from "gsap/TextPlugin";
  import { ScrollTrigger } from "gsap/ScrollTrigger";

  gsap.registerPlugin(TextPlugin, ScrollTrigger);

  window.addEventListener("DOMContentLoaded", () => {
    const headingEl = document.querySelector(".why-heading");
    const paraEl = document.querySelector(".why-para");

    if (!headingEl || !paraEl) return;

    const headingText = headingEl.textContent || "";
    headingEl.textContent = "";

    gsap
      .timeline({
        scrollTrigger: {
          trigger: headingEl.closest("section") || headingEl,
          start: "top 75%", // adjust when animation starts
          once: true,
        },
        delay: 0.3,
      })
      .to(headingEl, {
        text: headingText,
        duration: 2,
        ease: "none",
      })
      .from(
        paraEl,
        {
          opacity: 0,
          y: 20,
          duration: 0.8,
          ease: "power2.out",
        },
        "+=0.3"
      );
  });
</script>
